---
import "../styles/reset.css";
import "../styles/theme.css";
import "../styles/spacing.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

export interface Props {
    title: string;
    seo_img: string;
}

const { title, seo_img } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <!-- Primary Meta Tags -->
        <title>{title}</title>
        <meta name="title" content="Science Lab - Phú Yên" />
        <meta
            name="description"
            content="Dự án Science Lab được thành lập nhằm xóa bỏ rào cản STEM cho học sinh THCS & THPT ở Phú Yên bằng cách tổ chức các hoạt động STEM kết hợp trực tuyến và trực tiếp trên địa bàn tỉnh"
        />

        <!-- Open Graph / Facebook -->
        <meta property="og:type" content="website" />
        <meta property="og:url" content={Astro.url} />
        <meta property="og:title" content="Science Lab - Phú Yên" />
        <meta
            property="og:description"
            content="Dự án Science Lab được thành lập nhằm xóa bỏ rào cản STEM cho học sinh THCS & THPT ở Phú Yên bằng cách tổ chức các hoạt động STEM kết hợp trực tuyến và trực tiếp trên địa bàn tỉnh"
        />
        <meta
            property="og:image"
            content={seo_img}
        />

        <!-- Twitter -->
        <meta property="twitter:card" content="summary_large_image" />
        <meta property="twitter:url" content={Astro.url} />
        <meta property="twitter:title" content="Science Lab - Phú Yên" />
        <meta
            property="twitter:description"
            content="Dự án Science Lab được thành lập nhằm xóa bỏ rào cản STEM cho học sinh THCS & THPT ở Phú Yên bằng cách tổ chức các hoạt động STEM kết hợp trực tuyến và trực tiếp trên địa bàn tỉnh"
        />
        <meta
            property="twitter:image"
            content={seo_img}
        />
    </head>
    <body>
        <Header />
        <slot />
        <Footer />
    </body>
</html>

<script>
    const prepareAnimation = () => {
        const enableAnimation = (entries) => {
            entries.forEach((entry) => {
                const element = entry.target;
                if (entry.isIntersecting) {
                    element.removeAttribute("lazy-animation");
                    if (!element.hasAttribute("lazy-once")) return;
                    observer.unobserve(element);
                } else {
                    element.setAttribute("lazy-animation", "");
                }
            });
        };
        let options = {
            threshold: 0.8,
        };
        const observer = new IntersectionObserver(enableAnimation, options);
        const lazyAnimationElements =
            document.querySelectorAll("[lazy-animation]");
        lazyAnimationElements.forEach((el) => {
            observer.observe(el);
        });
    };
    document.addEventListener("DOMContentLoaded", prepareAnimation);
</script>

<style is:global>
    body {
        display: flex;
        flex-direction: column;
        overflow-x: hidden;
        background-color: var(--color-background);
        color: var(--color-text);
        box-sizing: border-box;
    }
    img {
        background-color: var(--color-text);
        transform: scale(1.001);
    }
    /* Hide scrollbar for Chrome, Safari and Opera */
    *[hide-scrollbar]::-webkit-scrollbar {
        display: none;
    }
    /* Hide scrollbar for IE, Edge and Firefox */
    *[hide-scrollbar] {
        -ms-overflow-style: none; /* IE and Edge */
        scrollbar-width: none; /* Firefox */
        scroll-behavior: smooth;
    }
    a[use-arrow]::after {
        content: " ↗";
    }
    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
        line-height: 100%;
    }
    h1 {
        font-size: var(--fontsize-larger);
        font-weight: bold;
    }
    h2 {
        font-size: var(--fontsize-large);
    }
    p {
        font-weight: 300;
        font-size: var(--fontsize-normal);
        max-width: fit-content;
        max-width: -moz-fit-content;
    }
    i {
        font-style: italic;
        font-weight: 100;
    }
    b {
        font-weight: bold;
    }
</style>
